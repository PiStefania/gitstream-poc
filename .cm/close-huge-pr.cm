# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  huge_pr:
    if:
      - {{ branch | estimatedReviewTime >= 60 }}
      - {{ files | match(regex=r/src\//) | some }}
    run:
      - action: add-label@v1
        args:
          label: 'huge-pr'
      - action: close@v1